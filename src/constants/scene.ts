import type { Tuple3 } from '@/types/common';

/** Default camera position for the scene. */
export const DEFAULT_CAMERA_POSITION: Tuple3 = [5, 4, 5];

/** Default camera field of view. */
export const DEFAULT_FOV = 50;

/** Colors used by interactive objects. */
export const OBJECT_COLORS = ['#ff6b35', '#4ecdc4', '#ffe66d', '#a855f7', '#f43f5e'] as const;
